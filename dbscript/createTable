create table if not exists COUPON
(
  COUPON_SEQ bigint not null
    constraint COUPON_pkey
      primary key,
  PHONE_NUMBER varchar(25),
  PERSONAL_INFORMATION_AGREEMENT_YN boolean default false,
    COUPON_NUMBER varchar(30),
  deleted boolean default false,
  reg_date timestamp
);